# トランスポートコントロール

> 楽曲の再生、停止、ループ、テンポ（BPM）などを制御する機能です。

## 概要
トランスポートコントロールは、タイムライン上の再生位置（プレイヘッド）を操作し、プロジェクトの再生状態を管理するために必要です。画面中央下のメインパネルに配置されています。

## 基本的な使い方
1. **▶ (Play)** ボタンをクリックして再生を開始します。再生中はボタンが **⏸ (Pause)** に変わります。
2. **⏸ (Pause)** ボタンをクリックして再生を一時停止します。
3. **⏹ (Stop)** ボタンをクリックすると、再生を停止しプレイヘッドを先頭（0.0）に戻します。
4. **🔁 (Loop)** ボタンをクリックして、ループ再生のオン/オフを切り替えます。ループがオンの場合、タイムラインの最後まで再生されると自動的に先頭から再生が再開されます。

## 詳細設定

| パラメータ | 説明 | 初期値 | 範囲 |
|:---|:---|:---|:---|
| BPM | プロジェクトのテンポ（1分間あたりのビート数）。再生速度に影響します。 | 120.0 | 20.0 〜 300.0 |
| Loop | オンにすると、再生がタイムラインの終端に達した際に先頭に戻って再生を続けます。 | On | On / Off |

## 関連項目
- [タイムライン & 波形表示](../user-guide/timeline.md)

## 注意事項・制限
> 現在のバージョンでは、BPMの変更は全体の再生速度に単純に比例して影響します。また、プレイヘッドの位置はパーセンテージ（0.0 〜 100.0）で管理されており、仮想的な秒数（Time）はプレイヘッド位置に基づいて計算・表示されます。
